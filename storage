<select name="category" id="categoryForm" class="custom-select" multiple>
  <% data.categories.forEach(function(category){ %>
  <option value="<%= category %>">
    <%= category %>
  </option>
  <% }); %>
</select>
</form>
